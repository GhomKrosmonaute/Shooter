{
  "name": "shooter",
  "version": "0.1",
  "license": "MIT",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "open": "open-cli http://localhost:5000/",
    "stop": "pm2 delete shooter",
    "bundle": "gulp bundle",
    "watch": "gulp watch",
    "format": "prettier --write src gulpfile.js tsconfig.json",
    "serve": "npm run open && npm run bundle && serve dist/"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "@types/pixi.js": "^5.0.0",
    "esbuild": "^0.8.31",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.3.0",
    "gulp-esbuild": "^0.3.14",
    "open-cli": "^6.0.1",
    "prettier": "^2.2.1",
    "serve": "^11.3.2",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "pixi.js": "^5.3.7"
  }
}
