{
  "name": "shooter",
  "version": "0.1",
  "license": "MIT",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "bundle": "esbuild cache/index.js --target=chrome58,firefox57,safari11,edge16 --bundle --outfile=dist/bundle.js",
    "format": "prettier --write src",
    "build": "tsc && npm run bundle",
    "start": "npm run build && serve dist/"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "@types/pixi.js": "^5.0.0",
    "esbuild": "^0.8.31",
    "prettier": "^2.2.1",
    "serve": "^11.3.2",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "pixi.js": "^5.3.7"
  }
}
